import React from 'react';
import Navigation from './Navigation';  // Ensure this is correct

const Header = () => {
  return (
    <header>
      <h1>This is the Header</h1>
      <Navigation />
    </header>
  );
};

export default Header;
